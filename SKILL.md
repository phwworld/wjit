---
name: commit
description: 팀 커밋 가이드에 맞춰 커밋 메시지 초안을 작성한다. 커밋을 직접 실행하지 않고 메시지만 제시한다. 커밋 직전이나 변경사항을 정리해 기록할 때 사용.
---

# 커밋 메시지 작성

팀 커밋 가이드에 맞춰 커밋 메시지 **초안을 작성한다.**
`git commit`을 직접 실행하지 않는다. 메시지를 제시하고, 사용자가 확인한 뒤 커밋한다.

## 절차

1. 변경 내용을 직접 확인한다. 추측으로 쓰지 않는다.

   ```bash
   git branch --show-current  # 보호 브랜치면 커밋 전에 알린다
   git status
   git diff --staged        # 스테이징된 변경이 있으면 이것을 기준으로
   git diff                 # 없으면 작업 트리 전체
   git log --oneline -5     # 최근 커밋의 톤 참고
   ```

   현재 브랜치가 `main` / `staging` / `develop`이면 커밋 메시지를 만들기 전에
   먼저 알리고 `feature/` 브랜치 분기를 제안한다.

2. 변경이 **하나의 작업**인지 판단한다.
   성격이 다른 변경이 섞여 있으면 메시지를 만들기 전에 **커밋 분리를 먼저 제안**한다.

3. 타입을 결정한다.

4. 형식에 맞춰 조립하고 체크리스트로 검증한 뒤 제시한다.

## 형식

```
<type>: <subject>
                          ← 빈 줄 (필수)
<body>
```

`type`, `subject`, `body` **세 가지 모두 필수다.** Subject만 쓴 커밋은 만들지 않는다.

**커밋 메시지에 이슈 번호를 넣지 않는다.** `Closes #45`, `Refs #67`, `#45` 같은 표기를
쓰지 않는다. 이슈 연결은 **MR 설명에서** 처리한다.

도구가 자동으로 붙이는 서명·생성 표시도 넣지 않는다.

## 타입 — 아래 6개만 사용한다

| 타입 | 사용 상황 |
|---|---|
| `feat` | 새로운 기능 추가, 또는 사용자 입장에서 동작이 달라지는 의도적 변경 |
| `fix` | 의도하지 않은 잘못된 동작(버그)을 원래대로 고쳤을 때 |
| `docs` | README, Swagger 등 **코드 외부 문서** 변경 |
| `refactor` | 기능 동작은 그대로, 구조·가독성·주석 개선 |
| `test` | 테스트 코드만 추가·수정 |
| `chore` | 의존성, 빌드, CI/CD, `.gitignore` 등 기능과 무관한 작업 |

판단 순서:

```
없던 기능을 새로 만들었다                     → feat
기존 기능의 동작을 의도적으로 바꿨다 (버그 아님) → feat
잘못 동작하던 것을 원래대로 고쳤다              → fix
동작은 그대로, 구조·가독성·주석 개선            → refactor
README·Swagger 등 코드 외부 문서               → docs
테스트 코드만                                → test
의존성·빌드·CI/CD·설정 파일                   → chore
```

헷갈리는 경계:

- **소스 코드 내부의 로직 설명 주석** 추가는 `docs`가 아니라 `refactor`
- Swagger 애노테이션만 추가하고 로직이 그대로면 `docs`
- 기존 기능을 의도적으로 바꾼 것은 `fix`가 아니라 `feat`

## Subject

- **한국어**로 작성
- **50자 이내**
- **명사형**으로 마무리 (`~추가`, `~수정`, `~삭제`)
- **마침표 없음**
- `type` 뒤에 `: ` (콜론 + 공백 하나)

무엇을 했는지 알 수 없는 제목을 쓰지 않는다.

```
금지: fix: 버그 수정 / feat: 기능 추가 / chore: 작업
사용: fix: 결제 금액 계산 오류 수정
```

## Body

**모든 커밋에 필수다.**

- **무엇을** 변경했는지, **왜** 변경했는지를 쓴다.
- "어떻게 구현했는지"보다 **"왜 이 변경이 필요했는지"**에 집중한다.
- 서술형 권장. 길이 제한 없음.
- **변경 파일 목록을 나열하지 않는다.** diff를 보면 알 수 있는 내용은 쓰지 않는다.

## 제시 전 검증

- [ ] 타입이 6개 중 하나이고 `: ` 뒤에 공백이 하나 있는가
- [ ] Subject가 50자 이내, 명사형, 마침표 없음인가
- [ ] Subject 아래에 빈 줄이 있는가
- [ ] Body가 있고 **"왜"**가 들어 있는가
- [ ] 이슈 번호나 도구 서명이 섞여 있지 않은가
- [ ] 하나의 작업만 담고 있는가

## 예시

```
feat: 로그인 실패 횟수 제한 기능 추가

보안 취약점 검토 결과, 무작위 대입 공격에 대한 방어 기능이 없는 것으로
확인되었습니다. 이를 방지하기 위해 로그인 실패 횟수 제한 기능을 추가하였습니다.
5회 이상 실패 시 해당 계정을 30분간 잠금 처리합니다.
```

```
refactor: 회원 등급 할인 계산 로직 DiscountCalculator 클래스로 분리

할인 계산 로직이 여러 서비스에 중복 구현되어 있어 유지보수에 어려움이
있었습니다. DiscountCalculator 클래스로 분리하여 향후 정책 변경 시
단일 지점에서 수정할 수 있도록 개선하였습니다.
```

```
chore: QueryDSL 의존성 추가

복잡한 동적 조회 쿼리를 타입 안전하게 작성하기 위해 QueryDSL을 도입하였습니다.
기존 JPQL 문자열 쿼리는 점진적으로 전환할 예정입니다.
```
